{% extends 'base.html.twig' %}

{% block title %}DetailCommande{% endblock %}

{% block body %}
    <h1>DetailCommande</h1>
    <style>

        body{
         background-color:#69a238;  
           
    }
    </style>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_commande.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ detail_commande.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_detail_commande_index') }}"><button>Retour à la liste</button></a> 

    <a href="{{ path('admin_detail_commande_edit', {'id': detail_commande.id}) }}"><button>Modifier</button></a> 

    {{ include('admin_detail_commande/_delete_form.html.twig') }}
{% endblock %}
