{% extends 'base.html.twig' %}

{% block title %}DetailCommande index{% endblock %}

{% block body %}
    <h1>DetailCommande index</h1>
    <style>

        body{
         background-color:#69a238;  
           
    }
    </style>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for detail_commande in detail_commandes %}
            <tr>
                <td>{{ detail_commande.id }}</td>
                <td>{{ detail_commande.quantite }}</td>
                <td>
                    <a href="{{ path('admin_detail_commande_show', {'id': detail_commande.id}) }}"><button>Détail</button></a> 
                    <a href="{{ path('admin_detail_commande_edit', {'id': detail_commande.id}) }}"><button>Modifier</button></a> 
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_detail_commande_new') }}"><button>Création</button></a> 
{% endblock %}
